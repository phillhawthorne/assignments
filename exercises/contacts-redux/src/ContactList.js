import React from 'react';

function ContactList (props){
    return(
        <div>
            {/* map through contacts and render <Contact/> for each one,
            passing in the necessary props */}
        </div>
    )
}

export default ContactList;